type Project {
    id: ID! @unique
    createdAt: DateTime!
    title: String!
    description: String
    repositoryUrl: String
    projectUrl: String
    createdBy: User
}

type User {
    id: ID! @unique
    name: String!
    email: String! @unique
    password: String!
    projects: [Project!]!
}